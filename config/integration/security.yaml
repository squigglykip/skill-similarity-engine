# =============================================================================
# Security Configuration (Future Extension)
# =============================================================================
# 
# Extracted from: config/architectural_config.yaml (security section)
# Purpose: Security settings for data protection and access control
# =============================================================================

# Data protection
data_protection:
  anonymize_employee_data: false
  encrypt_sensitive_fields: false
  audit_data_access: false

# Access control
access_control:
  require_authentication: false
  role_based_access: false
  api_key_authentication: false 